[package]
name = "cpuid-bit-structs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bit-struct = "0.3.0"
serde = { version = "1.0.152", features = ["alloc"] }
